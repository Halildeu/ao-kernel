# CNS-20260414-011 — Final Consensus (2026-04-14)

**Topic:** PR-C6a (Memory read MCP tool) — implementation-level adversarial review

**Mode:** Adversarial design review (**3 iterations**, final verdict `ready_for_impl: true`)

**Participants:** Claude (initiator), Codex (counterparty)

**Outcome:** AGREED after 3 rounds. Plan v3 implementation-ready.

**Scope:** PR-C6a ve ilgili parçalar (CNS-010 plan seviyesindeydi; bu istişare kod yazmadan önce son kontrol).

---

## İterasyon Özeti

| İter | Verdict | Blocking | Warning | ready_for_impl |
|---|---|---|---|---|
| İter-1 | PARTIAL | 3 | 6 | false |
| İter-2 | PARTIAL | 2 | 3 | false |
| **İter-3** | **AGREE** | **0** | **2** | **TRUE** |

Adversarial stats:
- Claude'un iter-1 tezi: 0/3 survived untouched (project memory "grep before accepting" kuralı yine tuttu)
- Toplam blocking absorbe: 5 (3 iter-1 + 2 iter-2)
- Toplam warning absorbe: 9 (6 iter-1 + 3 iter-2)
- Kalan 2 warning: implementation hygiene, blocker değil

---

### İter-1 (PARTIAL, 3 blocking + 6 warning)

Claude'un orijinal plan v1 tezi (CNS-010 consensus'a dayalı C6a scope'u). Codex blocking'leri:

1. **B1:** Workspace override zinciri uçtan uca tutarlı değil — `_with_evidence` wrapper ve `call_tool` implicit promote yolu `_find_workspace_root()` kullanıyor (param-aware değil)
2. **B2:** `ao_memory_read` implicit promote hattı yüzünden fiilen write yapar — `extract_from_tool_result` her envelope'u promote ediyor, confidence=0.95
3. **B3:** B4 auto-wire evidence, integrity manifest iddiasını karşılamıyor — `record_mcp_event` sadece JSONL+fsync, SHA256 manifest yok

Warning'ler: Global RL (W1), `get_rate_limiter` helper reuse yanlış (W2), pattern semantik uyumu (W3), resolver edge case'ler (W4), test matrisi eksik (W5), schema validation loader'a otomatik bağlı değil (W6).

### İter-2 (PARTIAL, 2 blocking + 3 warning)

Claude plan v2 (3 blocker çözümlü + 6 warning absorbe). Codex blocking'leri:

1. **B1:** Explicit hatalı `workspace_root` fallback'e düşerse B1 yeniden açılıyor — plan v2 resolver malformed value'yu fallback'e düşürüyor, bu durumda `_with_evidence` ve `call_tool()` tekrar CWD workspace'ine kayar
2. **B2:** Global `decision_extractor` metadata-skip shared contract bozuyor — `extract_from_tool_result` MCP dışı `llm.py` akışında da kullanılıyor, `test_memory_pipeline.py:121-132` `allowed`/`decision` extraction davranışını testle kilitliyor

Warning'ler: Pattern glob-on-glob tam subset değil (W1), manifest scope pivotu tüm yüzeylerde aynı anda güncellenmeli (W2), schema naming repo konvansiyonuyla hizalanmalı (W3).

### İter-3 (AGREE, 0 blocking + 2 warning, **ready_for_impl=true**)

Claude plan v3 (iter-2 blocker'larına cevap). Codex: yeni blocker yok. Kalan 2 warning implementation hygiene:

1. **W1:** Strict resolver kapsamı memory tool yollarıyla sınırlı tutulmalı — `_resolve_workspace_for_call()` public `workspace.py` kontratına taşınmaz, diğer tool'lara kör uygulanmaz
2. **W2:** C6a ile tool sayısı (5→6) ve MCP evidence anlatımı birkaç yüzeyde aynı anda eskir — README, CLAUDE.md, mcp_server docstring, test_mcp_server hard-coded count aynı batchte güncellenmeli

---

## Final Plan v3 (Implementation-ready)

### B1 Çözümü: Fallback yalnızca key-absent

```python
def _resolve_workspace_for_call(
    params: dict[str, Any] | None,
    *,
    fallback: Callable[[], Path | None] | None = None,
) -> Path | None:
    """Param-aware workspace resolver.

    Fallback policy: ONLY when `workspace_root` key is absent.
    Present-but-invalid → None (explicit deny path, NOT fallback).
    """
    if not isinstance(params, dict):
        return fallback() if fallback else _find_workspace_root()
    if "workspace_root" not in params:
        return fallback() if fallback else _find_workspace_root()
    raw = params["workspace_root"]
    if not isinstance(raw, str):
        return None
    raw = raw.strip()  # whitespace footgun guard (iter-3 additional rec)
    if not raw:
        return None
    try:
        candidate = Path(raw).expanduser().resolve(strict=False)
    except (OSError, RuntimeError):
        return None
    if candidate.name == ".ao":
        candidate = candidate.parent
    if not candidate.is_dir() or not (candidate / ".ao").is_dir():
        return None
    return candidate
```

**Çağrılan 3 yer (param-aware ortaklaştırma):**
1. `handle_memory_read` (yeni handler)
2. `_with_evidence` wrapper (`mcp_server.py:549` — `_find_workspace_root()` yerine)
3. `call_tool` implicit promote yolu (`mcp_server.py:672` — `_find_workspace_root()` yerine)

**Policy override köprüsü:**
```python
policy_workspace = ws / ".ao" if (ws / ".ao").is_dir() else ws
```

### B2 Çözümü: Sadece lokal denylist

Shared `decision_extractor.py` değişmez (iter-2 regresyon riski nedeniyle iptal edildi).

```python
# _internal/mcp/memory_tools.py module-level
_IMPLICIT_PROMOTE_SKIP: set[str] = {"ao_memory_read"}

# mcp_server.py::call_tool içinde:
if name not in _IMPLICIT_PROMOTE_SKIP:
    decisions = extract_from_tool_result(name, result)
    for d in decisions:
        if d.confidence >= 0.8:
            promote_decision(ws_root, ...)
```

**Not:** `decision_extractor.py:95` top-level scalar only (`isinstance(value, (str, int, float, bool))`) — `data.count` nested olduğundan bugün zaten promote edilmez. Ek katman gereksiz.

### B3 Çözümü: Scope pivot batch

Manifest iddiası C6a dışında kalır. **Aynı PR'da 3 yüzey eş güncellenir:**

1. `CLAUDE.md` §2 invariant #2: `JSONL append-only log (MCP events) + SHA256 integrity manifest (workspace artifacts)` — ikili form açıkça
2. `ao_kernel/_internal/evidence/mcp_event_log.py` module docstring: "MCP evidence = JSONL append + fsync (no manifest; manifest reserved for workspace artifacts, follow-up)"
3. `.claude/plans/SESSION-HANDOFF-TRANCHE-C-MID.md` Technical Debt satırı: "MCP evidence manifest — Tranş D (v3.1.0+)"

Manifest work ayrı iş (C8 sonrası veya Tranş D).

### W1 (iter-2) Çözümü: fnmatch direction

```python
import fnmatch as _fn
allowed_patterns: list[str] = policy["read"]["allowed_patterns"]
if not any(_fn.fnmatchcase(user_pattern, allowed) for allowed in allowed_patterns):
    return _deny_envelope(...)
```

Direction: `fnmatchcase(user_pattern, allowed_pattern)` — authorize edilen kullanıcının pattern'i. Ters yön over-allow üretir. Policy docstring'e "simple prefix-glob kısıtı önerilir; pattern-on-pattern yaklaşık subset kontrolü" notu.

### W3 (iter-2) Çözümü: Schema naming + `$schema` ref

- Dosya adı: `ao_kernel/defaults/schemas/policy-mcp-memory.schema.v1.json` (repo konvansiyonu)
- Policy JSON'a: `"$schema": "../schemas/policy-mcp-memory.schema.v1.json"` (relative path)
- Schema dosyasına: `"$id": "urn:ao:policy-mcp-memory:v1"` (kimlik)
- Handler private helper: `_load_memory_policy_validated(workspace)` içinde `load_with_override` + schema load + `jsonschema.validate()`

### Rate Limit (iter-1 W1/W2 + iter-3 accepted)

Scope key: `(resolved_workspace_absolute_path_str, operation)` tuple.

```python
_memory_rate_limiters: dict[tuple[str, str], TokenBucketRateLimiter] = {}
_memory_rl_lock = threading.Lock()

def _memory_rate_limiter_for(ws: Path, op: str, *, rpm: int) -> TokenBucketRateLimiter:
    key = (str(ws), op)
    with _memory_rl_lock:
        if key not in _memory_rate_limiters:
            _memory_rate_limiters[key] = TokenBucketRateLimiter(rps=rpm / 60.0)
        return _memory_rate_limiters[key]

def _memory_rate_limit_reset() -> None:
    """Test helper — separate from provider rate limiter reset_all()."""
    with _memory_rl_lock:
        _memory_rate_limiters.clear()
```

`TokenBucketRateLimiter` sınıfı `ao_kernel/_internal/prj_kernel_api/rate_limiter.py`'den reuse edilir; `get_rate_limiter()` helper kullanılmaz (provider state ile paylaşım yok).

### Test Matrisi (13 test)

1. Read enabled=true + pattern match → returns items (happy path)
2. Read enabled=false (default) → deny envelope `read_disabled_by_policy`
3. `workspace_root` param honored (valid path)
4. `workspace_root` absent + library mode → deny `workspace_not_found`
5. `workspace_root` present-but-invalid (`None`) → deny (fallback YOK — B1 doğrulaması)
6. `workspace_root` present-but-invalid (`123` int) → deny
7. `workspace_root` present-but-invalid (`""` empty) → deny
8. `workspace_root` `.ao`-suffix normalize (parent'a düşer)
9. Pattern not in `allowed_patterns` → deny `pattern_not_allowed`
10. Rate limit triggers (N+1th call → deny `rate_limit_exceeded`)
11. Workspace RL isolation (A-workspace quota B'yi etkilemez — W1 doğrulaması)
12. Implicit promote **skip** edildi (B2 doğrulaması: canonical store'a read tool sonucu yazmadı)
13. Evidence JSONL append edildi + param-aware workspace (B1 doğrulaması)

---

## 2 Mimari Karar

### Karar 1: Helper Placement → `_internal/mcp/memory_tools.py`

**Gerekçe:** `mcp_server.py` mevcut 756 LOC; C6a +230 LOC + C6b +300 LOC = 1286 LOC → CLAUDE.md §12 bütçesi (< 800) patlar.

Alt modül içeriği:
- `_resolve_workspace_for_call` (handler + call_tool + _with_evidence'dan kullanılır)
- `_IMPLICIT_PROMOTE_SKIP` (set)
- `_memory_rate_limiter_for`, `_memory_rate_limit_reset`
- `_load_memory_policy_validated`
- `handle_memory_read` (C6a)
- `handle_memory_write` (C6b, sonraki PR'da eklenecek)

`ao_kernel/_internal/mcp/__init__.py` package marker olarak eklenir.

### Karar 2: Release Stratejisi → PR-level incremental + bundle release

- C6a ayrı PR, C6b ayrı PR — her biri bağımsız merge
- v3.0.0 tag, her ikisi main'de olduktan sonra atılır
- CLAUDE.md §14 pattern `Tranş C = v3.0.0` korunur
- User'a SemVer sinyali tutarlı (v3.0.0 read-only, v3.1.0 write = minor'da major API eklemesi olmaz)

Codex iter-3 cevabı: "PR-level incremental + bundle release bu aşamada semver açısından daha temiz."

---

## Codex'in Kabul Ettiği (Claude korudu)

- `ToolSpec(allowed=True)` + handler-level gate (bypass pattern imkansız, authorize early-returns)
- Tek `policy_mcp_memory.v1.json` (read+write+rate_limit)
- `data: {items, count}` envelope shape
- `_resolve_workspace_for_call` private/internal scope (public değil)
- Library mode → deny envelope (exception propagate değil)
- `.ao` parent normalize + `policy_workspace` köprüsü
- `(resolved_ws, op)` tuple RL key + ayrı registry + ayrı reset
- `TokenBucketRateLimiter` sınıf reuse (helper kullanılmaz)
- Batch docs update (CLAUDE.md + mcp_event_log.py docstring + handoff debt)
- `_internal/mcp/memory_tools.py` sub-module
- PR-level incremental + bundle release

---

## Additional Recommendations (iter-3)

1. `_resolve_workspace_for_call()` içinde `raw.strip()` sonra `Path(raw)` — whitespace footgun guard
2. `ao_kernel/_internal/mcp/__init__.py` package marker ekle
3. C6b handoff metnindeki `policy_mcp_tool_calling.v1.json` adı repo ailesiyle hizalanmalı — muhtemelen genişletilecek dosya `policy_tool_calling.v1.json`
4. C6b geldiğinde `ao_memory_write` sonucunun implicit promotion'a girip girmemesi response shape üzerinden değerlendirilsin (C6a blocker değil)

---

## Implementation Hygiene (kalan 2 warning)

### W1 — Strict resolver scope sınırlı

`_resolve_workspace_for_call()`:
- ❌ Public `workspace.py` kontratına taşınmaz
- ❌ Mevcut tool'lara (ao_policy_check, ao_llm_route, ao_llm_call, ao_quality_gate, ao_workspace_status) kör uygulanmaz
- ✅ Memory read/write handler'ı + onun evidence/promote path'inde ortaklaştırılır
- ✅ Diğer tool'ların workspace_root semantiği ayrı audit edilirse ilerde genişletilir

### W2 — Batch docs update

Aynı PR'da 4 yüzey eş güncellenir (eski iddialar kalmamalı):

| Yüzey | Satır | Mevcut | Yeni |
|---|---|---|---|
| `README.md` | 126-150 | MCP tool listesi 5 tool | 6 tool (`ao_memory_read` eklenir) |
| `README.md` | 211 | Evidence trail (JSONL) matrisi | 6 tool satırı |
| `CLAUDE.md` | 55 | "5 governance tool" | "6 governance tool" |
| `mcp_server.py` | 6, 576 | docstring/comment "5 tools" | "6 tools" |
| `tests/test_mcp_server.py` | 524 | `len(TOOL_DEFINITIONS) == 5` | `== 6` |

---

## Implementation Sequence (PR-C6a)

Detaylı plan: `.claude/plans/PR-C6a-IMPLEMENTATION-PLAN.md`

```
1. Branch setup (claude/vigorous-ptolemy worktree'de veya yeni branch)
2. Yeni dosyalar: _internal/mcp/__init__.py, memory_tools.py, policy_mcp_memory.v1.json, policy-mcp-memory.schema.v1.json, test_mcp_memory_read.py
3. Mevcut dosyalar: mcp_server.py, CLAUDE.md, README.md, mcp_event_log.py, handoff plan
4. Test + ruff + mypy green
5. Commit + push + PR
6. M2 merge pattern (protection approval=0 → merge → approval=1)
```

---

**Status:** CONSENSUS REACHED. Implementation starts with PR-C6a per `.claude/plans/PR-C6a-IMPLEMENTATION-PLAN.md`.

**Adversarial stats:**
- Iterations: 3
- Blocking fixes absorbed: 5 (B1+B2+B3 iter-1, B1+B2 iter-2)
- Warning fixes absorbed: 9 (6 iter-1 + 3 iter-2)
- Claude's thesis revised: 3x (each iteration)
- Final `ready_for_impl`: **true**
