# CNS-20260414-012 — Final Consensus (2026-04-14)

**Topic:** PR-C6b (Memory write MCP tool + implicit promote policy refactor) — implementation-level adversarial review

**Mode:** Adversarial design review (**2 iterations**, final verdict `ready_for_impl: true`)

**Participants:** Claude (initiator), Codex (counterparty)

**Outcome:** AGREED after 2 rounds. Plan v2 implementation-ready.

**Scope:** PR-C6b ve implicit-promote policy refactor (CNS-010 iter-3 warning-4'ün hayata geçirilmesi).

---

## İterasyon Özeti

| İter | Verdict | Blocking | Warning | ready_for_impl |
|---|---|---|---|---|
| İter-1 | PARTIAL | 1 | 4 | false |
| **İter-2** | **AGREE** | **0** | **2** | **TRUE** |

Adversarial stats:
- Toplam blocking absorbe: 1 (iter-1)
- Toplam warning absorbe: 6 (4 iter-1 + 2 iter-2 hygiene)
- Claude'un plan v1 tezi: 1/1 blocker'ı revize edildi (workspace override yolu)
- C6a (CNS-011) 3 iter gerekiyordu; C6b 2 iter — pattern olgunlaştı

### İter-1 (PARTIAL, 1 blocking + 4 warning)

Claude'un plan v1 tezi (`load_default` + hardcoded threshold pseudo-code). Codex:

1. **B1:** `load_default()` workspace override okumuyor — plan v1 pseudo-code aynı hatayı yapıyor, test #9 (workspace-specific threshold) bu haliyle çalışmaz. `load_with_override()` gerekir.
2. **W1:** Skip gerekçesi yanlış ifade — doğru dayanak: envelope top-level `api_version`/`tool`/`allowed`/`decision` `extract_from_tool_result` tarafından scalar olarak promote edilir (nested `data.*` değil)
3. **W2:** `_SERVER_SIDE_CONFIDENCE = 0.75` repo default ile hizasız — `promote_decision` ve `CanonicalDecision` default'u 0.8
4. **W3:** `policy_tool_calling` schema runtime validation yok — memory policy gibi explicit validate gerek
5. **W4:** Test matrisi eksik — write-side workspace absent/invalid, evidence JSONL, `value_not_serializable`, skip'in canonical store yokluğu assertion

### İter-2 (AGREE, 0 blocking + 2 warning, **ready_for_impl=true**)

Claude plan v2 (B1 `_load_tool_calling_policy_validated` helper + 4 warning absorbe). Codex: yeni blocker yok. Kalan 2 hygiene warning:

1. **W1 (hygiene):** Evidence JSONL testi sadece mevcudiyet değil `tool`/`allowed`/`decision` alanlarını da assert etsin
2. **W2 (hygiene):** `policy_tool_calling.v1.json` iki yükleme semantiği (gateway bundled-only + implicit promote workspace-aware) — docstring/yorumla ayrımı netleştir

---

## Final Plan v2 (Implementation-ready)

### B1 Çözümü: `_load_tool_calling_policy_validated(ws)` helper

```python
# _internal/mcp/memory_tools.py
def _load_tool_calling_policy_validated(ws: Path | None) -> dict[str, Any]:
    """Load policy_tool_calling.v1.json with workspace override + schema validation.

    Note: this loader is used ONLY by the implicit-promote path in
    ``mcp_server.call_tool`` so that workspace overrides can tune
    ``implicit_canonical_promote`` per project. The gateway constructor
    in ``mcp_server.create_tool_gateway`` still uses ``load_default``
    because the gateway-level tool policy is a process-wide fallback,
    not a per-workspace override.
    """
    import jsonschema
    from ao_kernel.config import load_default, load_with_override
    policy_workspace = ws / ".ao" if (ws is not None and (ws / ".ao").is_dir()) else ws
    policy = load_with_override(
        "policies", "policy_tool_calling.v1.json",
        workspace=policy_workspace,
    )
    schema = load_default("schemas", "policy-tool-calling.schema.v1.json")
    jsonschema.validate(policy, schema)
    return policy
```

`mcp_server.call_tool` implicit promote bloğu:
```python
from ao_kernel._internal.mcp.memory_tools import (
    _IMPLICIT_PROMOTE_SKIP,
    _resolve_workspace_for_call,
    _load_tool_calling_policy_validated,
)
ws_root = _resolve_workspace_for_call(arguments or {}, fallback=_find_workspace_root)
if ws_root is not None and name not in _IMPLICIT_PROMOTE_SKIP:
    try:
        tool_policy = _load_tool_calling_policy_validated(ws_root)
    except Exception:
        # Fail-open on load/validate error — revert to bundled/default
        # implicit behavior. This is side-channel wiring; the MCP response
        # has already been delivered.
        tool_policy = {}
    implicit_cfg = tool_policy.get("implicit_canonical_promote") or {}
    if implicit_cfg.get("enabled", True):
        threshold = float(implicit_cfg.get("threshold", 0.8))
        source_prefix = implicit_cfg.get("source_prefix", "mcp:tool_result")
        decisions = extract_from_tool_result(name, result)
        for d in decisions:
            if d.confidence >= threshold:
                promote_decision(ws_root, key=d.key, value=d.value, source=source_prefix, confidence=d.confidence)
```

### Handler: `handle_memory_write`

```python
# _internal/mcp/memory_tools.py
_SERVER_SIDE_CONFIDENCE = 0.8  # Aligned with promote_decision default (canonical_store.py:281).
                                # Caller-supplied confidence is NOT trusted (CNS-010 iter-3 Q9).

_IMPLICIT_PROMOTE_SKIP: set[str] = {"ao_memory_read", "ao_memory_write"}
# Both tools return envelopes whose top-level scalar fields
# (api_version, tool, allowed, decision) would otherwise be
# extracted by decision_extractor.extract_from_tool_result and
# promoted as tool.ao_memory_*.{api_version,tool,allowed,decision}
# — self-referential noise. ao_memory_write also promotes
# explicitly via promote_decision(), so implicit promotion would
# double-write. Both must be denylisted.

def handle_memory_write(params: dict[str, Any]) -> dict[str, Any]:
    from ao_kernel.context.canonical_store import promote_decision
    from ao_kernel.mcp_server import _find_workspace_root

    tool = "ao_memory_write"
    ws = _resolve_workspace_for_call(params, fallback=_find_workspace_root)
    if ws is None:
        return _deny(tool, "workspace_not_found")
    try:
        policy = _load_memory_policy_validated(ws)
    except Exception as exc:
        return _deny(tool, "policy_load_error", error=str(exc))
    write_cfg = policy.get("write", {})
    if not isinstance(write_cfg, dict) or not bool(write_cfg.get("enabled", False)):
        return _deny(tool, "write_disabled_by_policy")
    key = params.get("key") if isinstance(params, dict) else None
    value = params.get("value", None) if isinstance(params, dict) else None
    source = params.get("source", "mcp:tool_write") if isinstance(params, dict) else "mcp:tool_write"
    if not isinstance(key, str) or not key.strip():
        return _deny(tool, "invalid_key")
    if value is None:
        return _deny(tool, "invalid_value")
    if not isinstance(source, str) or not source.strip():
        return _deny(tool, "invalid_source")
    key_prefixes = write_cfg.get("allowed_key_prefixes", [])
    if not isinstance(key_prefixes, list) or not key_prefixes:
        return _deny(tool, "key_prefix_not_allowed")
    if not any(isinstance(p, str) and key.startswith(p) for p in key_prefixes):
        return _deny(tool, "key_prefix_not_allowed")
    source_prefixes = write_cfg.get("allowed_source_prefixes", ["mcp:"])
    if not any(isinstance(p, str) and source.startswith(p) for p in source_prefixes):
        return _deny(tool, "source_prefix_not_allowed")
    max_bytes = int(write_cfg.get("max_value_bytes", 4096))
    try:
        encoded = json.dumps(value).encode("utf-8")
    except (TypeError, ValueError) as exc:
        return _deny(tool, "value_not_serializable", error=str(exc))
    if len(encoded) > max_bytes:
        return _deny(tool, "oversize")
    rpm = int(policy.get("rate_limit", {}).get("writes_per_minute", 10))
    limiter = _memory_rate_limiter_for(ws, "write", rpm=rpm)
    if not limiter.try_acquire():
        return _deny(tool, "rate_limit_exceeded")
    try:
        decision = promote_decision(
            ws, key=key, value=value, source=source,
            confidence=_SERVER_SIDE_CONFIDENCE,
        )
    except Exception as exc:
        return _error(tool, f"promote_failure: {exc}")
    return {
        "api_version": _API_VERSION,
        "tool": tool,
        "allowed": True,
        "decision": "executed",
        "reason_codes": [],
        "data": {"key": decision.key, "confidence": decision.confidence, "promoted_at": decision.promoted_at},
        "error": None,
    }
```

### Policy + Schema genişletme

**`policy_tool_calling.v1.json`** (mevcut dosya, yeni block eklenir):
```json
{
  "version": "v1",
  "enabled": false,
  "max_tool_calls_per_request": 5,
  "max_tool_rounds": 3,
  "allowed_tools": [],
  "blocked_tools": [],
  "tool_permissions": {
    "default": "read_only",
    "mutating_requires_confirmation": true
  },
  "cycle_detection": {
    "enabled": true,
    "max_identical_calls": 2
  },
  "fail_action": "block",
  "implicit_canonical_promote": {
    "enabled": true,
    "threshold": 0.8,
    "source_prefix": "mcp:tool_result"
  }
}
```

**`policy-tool-calling.schema.v1.json`** (mevcut dosya, properties'e `implicit_canonical_promote` eklenir — required değil, legacy override güvenliği):
```json
"implicit_canonical_promote": {
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "enabled": {"type": "boolean"},
    "threshold": {"type": "number", "minimum": 0.0, "maximum": 1.0},
    "source_prefix": {"type": "string", "minLength": 1}
  }
}
```

**Not:** `implicit_canonical_promote` top-level required değil; sub-field'lar da optional — `load_with_override` deep-merge yapmadığı için legacy workspace override'ları korunur.

### Test Matrisi (14 yeni)

1. Write enabled + matching prefixes → executed via CAS
2. Write disabled (default) → deny `write_disabled_by_policy`
3. Empty `allowed_key_prefixes` → deny
4. Key prefix miss → deny `key_prefix_not_allowed`
5. Oversized value → deny `oversize`
6. Bad source prefix → deny `source_prefix_not_allowed`
7. Caller confidence ignored (server sets 0.8)
8. Rate limit triggers (writes_per_minute bucket)
9. Implicit promote threshold from workspace-override policy (set threshold=0.99 → 0.95-confidence decision skipped)
10. `ao_memory_write` in `_IMPLICIT_PROMOTE_SKIP` (membership assertion)
11. Write `workspace_root` absent + library mode → deny `workspace_not_found`
12. Write `workspace_root` present-but-invalid (None / int / empty) → deny (no fallback)
13. Evidence JSONL lands in param-aware workspace + assert `tool="ao_memory_write"`, `allowed=True`, `decision="executed"` (W1 hygiene absorbe)
14. `value_not_serializable` (ör. `value=set()`) → deny `value_not_serializable`

Ek skip-effect test:
- Explicit write executed → canonical store `mem.foo` key'i var, `tool.ao_memory_write.*` key'leri YOK (iki seviye: üyelik + canonical assertion)

---

## 2 Mimari Karar (CNS-011'den taşındı + yeni)

### Karar 1: Helper placement → `_internal/mcp/memory_tools.py`

C6a pattern'iyle tutarlı. `_load_tool_calling_policy_validated`, `handle_memory_write` aynı modülde. Ayrı `tool_calling_policy.py` premature split. Codex iter-2 Q1 onayı.

### Karar 2: Implicit promote fail-open vs memory policy fail-closed

| Path | Failure mode | Why |
|---|---|---|
| Memory policy load (read/write handler) | **Fail-closed** — deny envelope | Main response path; user-visible outcome |
| Implicit promote policy load (`call_tool` side-channel) | **Fail-open** — default behavior | Side-channel canonical wiring; MCP response already delivered |

Codex iter-2 Q2 onayı: "revert to bundled/default implicit behavior" yorumu şart.

### Karar 3: `implicit_canonical_promote` optional + bundled default

- Schema'da optional (legacy override güvenliği)
- Bundled `policy_tool_calling.v1.json`'a ekle (operator görünürlüğü + debug)
- Runtime default `{enabled=True, threshold=0.8, source_prefix="mcp:tool_result"}`

Codex iter-2 Q3+Q4 onayı.

---

## Codex'in Kabul Ettiği (Claude korudu)

- `memory_tools.py` landing zone — `handle_memory_write` + `_load_tool_calling_policy_validated` aynı modül
- `policy_tool_calling.v1.json` aile — ayrı `policy_mcp_implicit_promote.v1.json` açılmaz
- `promote_decision` CAS route (public) — ayrı public CAS helper yüzeyi açılmaz
- `allow_overwrite=True` default — write tool explicit `False + expected_revision=None` ile her write conflict üretmesin
- `max_value_bytes` JSON-encoded ölçüm — store fiilen JSON yazıyor
- `ao_memory_write` skip gerekli (doğru gerekçe iter-1 W1 düzeltmesiyle)
- Server-side confidence 0.8 (repo default ile hizalı)
- Revision field C6c'ye ertelendi (`CanonicalDecision` revision taşımıyor, extra API gerektirir)
- Denylist grow stratejisi yeterli (inverse allowlist over-engineering)

---

## Additional Recommendations (iter-2 hygiene)

1. `implicit_canonical_promote` schema'da subfield'lar optional (legacy override'lar için güvenli)
2. Fail-open branch yorumu: "revert to bundled/default implicit behavior"
3. `_SERVER_SIDE_CONFIDENCE = 0.8` named constant (handler yanında, literal arama kolay)
4. `source` etiketi grep: `"mcp_tool"` (eski hardcoded) vs `"mcp:tool_result"` (yeni) — batch'te tüm referansları doğrula
5. Evidence JSONL testi: `tool/allowed/decision` alanları assert edilmeli (presence-only yetmez)

---

## Implementation Sequence (PR-C6b)

Detaylı plan: `.claude/plans/PR-C6b-IMPLEMENTATION-PLAN.md`

```
1. Branch: mevcut worktree'de devam (claude/vigorous-ptolemy) veya claude/tranche-c-c6b
2. Yeni kod: handle_memory_write + _SERVER_SIDE_CONFIDENCE + _load_tool_calling_policy_validated
   (hepsi _internal/mcp/memory_tools.py içinde)
3. mcp_server.py patch: TOOL_DEFINITIONS + TOOL_DISPATCH + call_tool implicit promote refactor
4. Policy + schema genişletme: policy_tool_calling.v1.json + policy-tool-calling.schema.v1.json
5. Test suite: 14 yeni test + skip-effect assertion
6. Docs batch: README 7 tool, CLAUDE.md §4, mcp_server docstring, tool_gateway test count, handoff merge satırı
7. pytest + ruff + mypy yeşil
8. Commit + push + PR + CI 7/7 + M2 merge
```

---

**Status:** CONSENSUS REACHED. Implementation starts with PR-C6b per `.claude/plans/PR-C6b-IMPLEMENTATION-PLAN.md`.

**Adversarial stats:**
- Iterations: 2
- Blocking fixes absorbed: 1 (iter-1 B1)
- Warning fixes absorbed: 6 (4 iter-1 + 2 iter-2 hygiene)
- Claude's thesis revised: 1x (iter-2 plan v2)
- Final `ready_for_impl`: **true**
