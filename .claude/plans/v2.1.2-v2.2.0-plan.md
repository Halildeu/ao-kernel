# ao-kernel — v2.1.2 + v2.2.0 Planı

**Tarih:** 2026-04-13
**İstişare:** CNS-20260413-009 (Codex Verdict: D — split release)
**Başlangıç:** main @ 40c6acc (v2.1.1)

---

## v2.1.2 — Patch Release (4 iş)

### İş 1: CLAUDE.md doğruluk güncellemesi
- **Durum:** ✅ TAMAMLANDI (bu sohbette)
- Eski hatalı değerler kaldırıldı (567 test, 508 test, 34 dosya, "6 quality gate")
- Quality gate vs eval check ayrımı netleştirildi
- Değişken sayılar yerine nasıl bulunacağı yazıldı
- Stabil tasarım: her sohbette güncelleme gerekmez

### İş 2: Telemetry metric fix
- **Durum:** Bekliyor
- `record_context_compile()` → `items_included/items_excluded` parametreleri dead
- Çözüm: histogram/counter ekle (parametre silme DEĞİL — backward-compat)
- `record_context_compile` testi de ekle (mevcut telemetry testlerinde yok)
- Tahmini: ~2 test

### İş 3: Tool calling unit testleri
- **Durum:** Bekliyor
- `ao_kernel/_internal/prj_kernel_api/tool_calling.py` — 191 LOC, 0 test
- **Öncelik:** `extract_tool_calls()` (canlı yolda kullanılıyor) > `build_tools_param` > `build_tool_result`
- Senaryolar:
  - Claude tool_use extraction (normal + boş + malformed)
  - OpenAI Chat Completions tool_calls extraction
  - OpenAI Responses API function_call extraction
  - arguments JSON parse failure → empty dict fallback
  - Provider dispatch (claude vs openai)
  - Normalization: arguments → input key rename
- Tahmini: ~6 test

### İş 4: Evidence integrity testleri
- **Durum:** Bekliyor
- `ao_kernel/_internal/evidence/integrity_verify.py` — verify_run_dir() dalları
- Mevcut: 1 smoke test (empty dir)
- Eklenecek: OK path, MISSING file, MISMATCH hash, manifest-shape-invalid
- Tahmini: ~4 test

---

## v2.2.0 — Minor Release (1 iş, büyük kampanya)

### İş 5: Roadmap critical-slice testleri
- **Durum:** Bekliyor
- 9 modül, ~2,500 LOC, 0 gerçek test (sadece 4 facade import test)
- **Top-5 kritik modül** (Codex önerisi):
  1. `exec_steps.py` — safety mantığı: readonly enforcement, DLQ, immutability
  2. `step_templates.py` — dosya sistemi + subprocess yan etkileri
  3. `executor.py` — compile→evidence→execute orkestrasyon
  4. `compiler.py` — schema validation, deterministik plan
  5. `exec_evidence.py` — readonly baseline snapshot (safety-critical)
- İkinci dalga: `change_proposals.py` (exec_steps üzerinden dolaylı kapsanabilir)
- Test senaryoları (Codex önerisi):
  - compiler: milestone subset determinism, invalid schema rejection
  - executor: dry_run mode validation, DLQ/summary propagation
  - exec_steps: readonly cmd allowlist enforcement, core unlock blocked, milestone caps, run_cmd failure → DLQ
  - step_templates: file write safety, subprocess isolation
  - exec_evidence: readonly snapshot baseline, missing git fallback
- Tahmini: ~20 test

---

## Tamamlanmış ve Mükerrer Olmayan İşler

Aşağıdakiler daha önce "kalan iş" listesindeydi ama derin doğrulamada zaten yapılmış olduğu teyit edildi:

| Eski İş | Gerçek Durum |
|---------|-------------|
| Streaming tool_calls fix | 7 test var (test_tool_streaming.py) |
| Agent coordination + optimistic locking | 13 test var (test_agent_coordination.py) |
| profile_router testleri | 11 test var (test_context_compiler.py + test_chaos.py) |
| Fail-closed bug'lar | 26 exception handler audit → 0 gerçek bug |
| governance.py limits fail-open | Acceptable — non-numeric skip doğru davranış |
